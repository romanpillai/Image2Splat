# TEMPLATE ONLY - do not put a real key in this file.
# This file is committed; .env is the gitignored one that actually gets read.
#
#   copy .env.example .env      then edit .env
#
# Get a key: https://fal.ai/dashboard/keys
#
# A real FAL_KEY environment variable always takes precedence over .env,
# so exporting the key cannot be silently overridden by a stale file.

FAL_KEY=
