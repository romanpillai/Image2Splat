# image2SPLAT -- Python packages. Tested with Python 3.12 on Windows.
#
# Install PyTorch FIRST, as the CUDA build for your NVIDIA driver (see README):
#   pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu129
# then:
#   pip install -r requirements.txt
#
# Versions are the ones this project was built and tested against.
# Model weights are NOT packages: they download from Hugging Face on first use.

# --- web server
fastapi==0.141.1
uvicorn==0.52.1
pydantic==2.13.4

# --- images, video, geometry
numpy==2.4.4
opencv-python==5.0.0.93
scipy==1.18.0
pillow==11.3.0
imageio-ffmpeg==0.6.0          # ships its own ffmpeg; nothing else to install

# --- models
transformers==5.14.1           # BiRefNet, SAM 2.1, Grounding DINO
huggingface-hub==1.26.0
safetensors==0.8.0
onnxruntime==1.28.0            # RMBG-1.4
timm==1.0.28                   # needed by BiRefNet's model code
kornia==0.8.3                  # needed by BiRefNet's model code
einops==0.8.2                  # needed by BiRefNet's model code
# MoGe-2: depth and lens. Installed from GitHub, so Git must be on PATH.
utils3d-moge @ git+https://github.com/EasternJournalist/utils3d-moge.git@62f09d58509485564e24d5d9f6aac9ee9ebc0c37
moge @ git+https://github.com/microsoft/MoGe.git@74fbce054ebed49800de42d0ad0e83495065719a

# --- fal.ai video generation (needs FAL_KEY, see .env.example)
fal-client==1.0.1
